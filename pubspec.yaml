name: safar_kar
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.18.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # utilities
  get:
  liquid_swipe:

  # firebase
  firebase_core: ^2.17.0
  firebase_auth: ^4.10.1
  cloud_firestore: 

  # UI
  cupertino_icons: ^1.0.2
  flutter_otp_text_field:
  smooth_page_indicator:
  google_fonts:
  flutter_native_splash:
  line_awesome_flutter:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0


flutter_native_splash:
  color: "#f7f6f1"
  color_dark: "#272727"
  image: assets/images/TrainLogo.png
  image_dark: assets/images/TrainLogo.png

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/images/logo/
    - assets/images/onboard/
    - assets/images/welcome/